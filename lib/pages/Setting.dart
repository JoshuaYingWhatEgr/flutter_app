import 'package:flutter/material.dart';

class Setting_Pages extends StatefulWidget {
  const Setting_Pages({Key? key}) : super(key: key);

  @override
  State<StatefulWidget> createState() {
    return _Setting_Pages();
  }

}

class _Setting_Pages extends State<Setting_Pages> {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(
      child: Text('这是设置'),
    );
  }
}
